<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.vincent.crm.customer.mapper.ExtCustomerContactMapper">

    <!-- 根据客户 ID 查询联系人列表 -->
    <select id="selectByCustomerId" resultType="cn.vincent.crm.customer.domain.CustomerContact">
        SELECT id, customer_id, name, phone, email, position, department, is_primary, remark,
               organization_id, create_user, update_user, create_time, update_time
        FROM customer_contact
        WHERE customer_id = #{customerId}
        ORDER BY is_primary DESC, create_time ASC
    </select>

</mapper>
