<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.vincent.crm.system.mapper.OrganizationUserMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="cn.vincent.crm.system.domain.OrganizationUser">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="organization_id" property="organizationId"/>
        <result column="department_id" property="departmentId"/>
    </resultMap>

    <!-- 根据主键查询 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT id, user_id, organization_id, department_id
        FROM sys_organization_user
        WHERE id = #{id}
    </select>

    <!-- 根据主键批量查询 -->
    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT id, user_id, organization_id, department_id
        FROM sys_organization_user
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 新增 -->
    <insert id="insert" parameterType="cn.vincent.crm.system.domain.OrganizationUser">
        INSERT INTO sys_organization_user (id, user_id, organization_id, department_id)
        VALUES (#{id}, #{userId}, #{organizationId}, #{departmentId})
    </insert>

    <!-- 批量新增 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO sys_organization_user (id, user_id, organization_id, department_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.userId}, #{item.organizationId}, #{item.departmentId})
        </foreach>
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="cn.vincent.crm.system.domain.OrganizationUser">
        UPDATE sys_organization_user
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="organizationId != null">organization_id = #{organizationId},</if>
            <if test="departmentId != null">department_id = #{departmentId},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据主键批量删除 -->
    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM sys_organization_user
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
