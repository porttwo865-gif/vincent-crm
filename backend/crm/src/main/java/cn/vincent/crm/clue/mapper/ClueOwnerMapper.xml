<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.vincent.crm.clue.mapper.ClueOwnerMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="cn.vincent.crm.clue.domain.ClueOwner">
        <id column="id" property="id"/>
        <result column="clue_id" property="clueId"/>
        <result column="from_owner" property="fromOwner"/>
        <result column="to_owner" property="toOwner"/>
        <result column="operation_type" property="operationType"/>
        <result column="operator_id" property="operatorId"/>
        <result column="operate_time" property="operateTime"/>
        <result column="organization_id" property="organizationId"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, clue_id, from_owner, to_owner, operation_type, operator_id, operate_time,
        organization_id, create_user, update_user, create_time, update_time
    </sql>

    <!-- 根据主键查询 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM clue_owner
        WHERE id = #{id}
    </select>

    <!-- 根据主键批量查询 -->
    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM clue_owner
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 新增 -->
    <insert id="insert" parameterType="cn.vincent.crm.clue.domain.ClueOwner">
        INSERT INTO clue_owner (id, clue_id, from_owner, to_owner, operation_type,
                                operator_id, operate_time, organization_id,
                                create_user, update_user, create_time, update_time)
        VALUES (#{id}, #{clueId}, #{fromOwner}, #{toOwner}, #{operationType},
                #{operatorId}, #{operateTime}, #{organizationId},
                #{createUser}, #{updateUser}, #{createTime}, #{updateTime})
    </insert>

    <!-- 批量新增 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO clue_owner (id, clue_id, from_owner, to_owner, operation_type,
                                operator_id, operate_time, organization_id,
                                create_user, update_user, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.clueId}, #{item.fromOwner}, #{item.toOwner}, #{item.operationType},
             #{item.operatorId}, #{item.operateTime}, #{item.organizationId},
             #{item.createUser}, #{item.updateUser}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="cn.vincent.crm.clue.domain.ClueOwner">
        UPDATE clue_owner
        <set>
            <if test="clueId != null">clue_id = #{clueId},</if>
            <if test="fromOwner != null">from_owner = #{fromOwner},</if>
            <if test="toOwner != null">to_owner = #{toOwner},</if>
            <if test="operationType != null">operation_type = #{operationType},</if>
            <if test="operatorId != null">operator_id = #{operatorId},</if>
            <if test="operateTime != null">operate_time = #{operateTime},</if>
            <if test="organizationId != null">organization_id = #{organizationId},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据主键批量删除 -->
    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM clue_owner
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
