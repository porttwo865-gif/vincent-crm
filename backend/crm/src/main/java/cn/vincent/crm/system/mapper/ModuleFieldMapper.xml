<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.vincent.crm.system.mapper.ModuleFieldMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="cn.vincent.crm.system.domain.ModuleField">
        <id column="id" property="id"/>
        <result column="form_id" property="formId"/>
        <result column="form_key" property="formKey"/>
        <result column="name" property="name"/>
        <result column="field_key" property="fieldKey"/>
        <result column="field_type" property="fieldType"/>
        <result column="internal_key" property="internalKey"/>
        <result column="is_system" property="isSystem"/>
        <result column="required" property="required"/>
        <result column="default_value" property="defaultValue"/>
        <result column="options" property="options"/>
        <result column="sort" property="sort"/>
        <result column="visible" property="visible"/>
        <result column="editable" property="editable"/>
        <result column="section_name" property="sectionName"/>
        <result column="section_sort" property="sectionSort"/>
        <result column="organization_id" property="organizationId"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, form_id, form_key, name, field_key, field_type, internal_key, is_system,
        required, default_value, options, sort, visible, editable, section_name, section_sort,
        organization_id, create_user, update_user, create_time, update_time
    </sql>

    <!-- 根据主键查询 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM module_field
        WHERE id = #{id}
    </select>

    <!-- 根据主键批量查询 -->
    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM module_field
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 新增 -->
    <insert id="insert" parameterType="cn.vincent.crm.system.domain.ModuleField">
        INSERT INTO module_field (id, form_id, form_key, name, field_key, field_type, internal_key,
                                  is_system, required, default_value, options, sort, visible, editable,
                                  section_name, section_sort, organization_id, create_user, update_user,
                                  create_time, update_time)
        VALUES (#{id}, #{formId}, #{formKey}, #{name}, #{fieldKey}, #{fieldType}, #{internalKey},
                #{isSystem}, #{required}, #{defaultValue}, #{options}, #{sort}, #{visible}, #{editable},
                #{sectionName}, #{sectionSort}, #{organizationId}, #{createUser}, #{updateUser},
                #{createTime}, #{updateTime})
    </insert>

    <!-- 批量新增 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO module_field (id, form_id, form_key, name, field_key, field_type, internal_key,
                                  is_system, required, default_value, options, sort, visible, editable,
                                  section_name, section_sort, organization_id, create_user, update_user,
                                  create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.formId}, #{item.formKey}, #{item.name}, #{item.fieldKey}, #{item.fieldType},
             #{item.internalKey}, #{item.isSystem}, #{item.required}, #{item.defaultValue}, #{item.options},
             #{item.sort}, #{item.visible}, #{item.editable}, #{item.sectionName}, #{item.sectionSort},
             #{item.organizationId}, #{item.createUser}, #{item.updateUser}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="cn.vincent.crm.system.domain.ModuleField">
        UPDATE module_field
        <set>
            <if test="formId != null">form_id = #{formId},</if>
            <if test="formKey != null">form_key = #{formKey},</if>
            <if test="name != null">name = #{name},</if>
            <if test="fieldKey != null">field_key = #{fieldKey},</if>
            <if test="fieldType != null">field_type = #{fieldType},</if>
            <if test="internalKey != null">internal_key = #{internalKey},</if>
            <if test="isSystem != null">is_system = #{isSystem},</if>
            <if test="required != null">required = #{required},</if>
            <if test="defaultValue != null">default_value = #{defaultValue},</if>
            <if test="options != null">options = #{options},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="visible != null">visible = #{visible},</if>
            <if test="editable != null">editable = #{editable},</if>
            <if test="sectionName != null">section_name = #{sectionName},</if>
            <if test="sectionSort != null">section_sort = #{sectionSort},</if>
            <if test="organizationId != null">organization_id = #{organizationId},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据主键批量删除 -->
    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM module_field
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
