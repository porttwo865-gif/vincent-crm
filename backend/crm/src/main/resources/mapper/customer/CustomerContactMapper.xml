<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.vincent.crm.customer.mapper.CustomerContactMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="cn.vincent.crm.customer.domain.CustomerContact">
        <id column="id" property="id"/>
        <result column="customer_id" property="customerId"/>
        <result column="name" property="name"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="position" property="position"/>
        <result column="department" property="department"/>
        <result column="is_primary" property="isPrimary"/>
        <result column="remark" property="remark"/>
        <result column="organization_id" property="organizationId"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, customer_id, name, phone, email, position, department, is_primary, remark,
        organization_id, create_user, update_user, create_time, update_time
    </sql>

    <!-- 根据主键查询 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM customer_contact
        WHERE id = #{id}
    </select>

    <!-- 根据主键批量查询 -->
    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM customer_contact
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 新增 -->
    <insert id="insert" parameterType="cn.vincent.crm.customer.domain.CustomerContact">
        INSERT INTO customer_contact (id, customer_id, name, phone, email, position, department,
                                      is_primary, remark, organization_id,
                                      create_user, update_user, create_time, update_time)
        VALUES (#{id}, #{customerId}, #{name}, #{phone}, #{email}, #{position}, #{department},
                #{isPrimary}, #{remark}, #{organizationId},
                #{createUser}, #{updateUser}, #{createTime}, #{updateTime})
    </insert>

    <!-- 批量新增 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO customer_contact (id, customer_id, name, phone, email, position, department,
                                      is_primary, remark, organization_id,
                                      create_user, update_user, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.customerId}, #{item.name}, #{item.phone}, #{item.email}, #{item.position}, #{item.department},
             #{item.isPrimary}, #{item.remark}, #{item.organizationId},
             #{item.createUser}, #{item.updateUser}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="cn.vincent.crm.customer.domain.CustomerContact">
        UPDATE customer_contact
        <set>
            <if test="customerId != null">customer_id = #{customerId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="position != null">position = #{position},</if>
            <if test="department != null">department = #{department},</if>
            <if test="isPrimary != null">is_primary = #{isPrimary},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="organizationId != null">organization_id = #{organizationId},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据主键批量删除 -->
    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM customer_contact
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
