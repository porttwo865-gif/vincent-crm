<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.vincent.crm.clue.mapper.ExtClueOwnerMapper">

    <!-- 根据线索 ID 查询负责人变更历史列表 -->
    <select id="selectByClueId" resultType="cn.vincent.crm.clue.domain.ClueOwner">
        SELECT id, clue_id, from_owner, to_owner, operation_type, operator_id, operate_time,
               organization_id, create_user, update_user, create_time, update_time
        FROM clue_owner
        WHERE clue_id = #{clueId}
        ORDER BY create_time DESC
    </select>

</mapper>