<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.vincent.crm.contract.plan.mapper.PaymentPlanMapper">

    <resultMap id="BaseResultMap" type="cn.vincent.crm.contract.plan.domain.PaymentPlan">
        <id column="id" property="id"/>
        <result column="contract_id" property="contractId"/>
        <result column="plan_num" property="planNum"/>
        <result column="amount" property="amount"/>
        <result column="expected_date" property="expectedDate"/>
        <result column="actual_date" property="actualDate"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, contract_id, plan_num, amount, expected_date, actual_date, status, remark,
        create_user, update_user, create_time, update_time
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM crm_payment_plan
        WHERE id = #{id}
    </select>

    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM crm_payment_plan
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <insert id="insert" parameterType="cn.vincent.crm.contract.plan.domain.PaymentPlan">
        INSERT INTO crm_payment_plan (id, contract_id, plan_num, amount, expected_date, actual_date, status, remark,
                                      create_user, update_user, create_time, update_time)
        VALUES (#{id}, #{contractId}, #{planNum}, #{amount}, #{expectedDate}, #{actualDate}, #{status}, #{remark},
                #{createUser}, #{updateUser}, #{createTime}, #{updateTime})
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO crm_payment_plan (id, contract_id, plan_num, amount, expected_date, actual_date, status, remark,
                                      create_user, update_user, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.contractId}, #{item.planNum}, #{item.amount}, #{item.expectedDate}, #{item.actualDate}, #{item.status}, #{item.remark},
             #{item.createUser}, #{item.updateUser}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <update id="update" parameterType="cn.vincent.crm.contract.plan.domain.PaymentPlan">
        UPDATE crm_payment_plan
        <set>
            <if test="contractId != null">contract_id = #{contractId},</if>
            <if test="planNum != null">plan_num = #{planNum},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="expectedDate != null">expected_date = #{expectedDate},</if>
            <if test="actualDate != null">actual_date = #{actualDate},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM crm_payment_plan
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
