<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.vincent.crm.contract.record.mapper.ExtPaymentRecordMapper">

    <select id="selectByContractId" resultType="cn.vincent.crm.contract.record.domain.PaymentRecord">
        SELECT id, contract_id, plan_id, amount, payment_date, payment_method, remark,
               create_user, update_user, create_time, update_time
        FROM crm_payment_record
        WHERE contract_id = #{contractId}
        ORDER BY payment_date DESC, create_time DESC
    </select>

</mapper>
