<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.vincent.crm.order.mapper.OrderItemMapper">

    <resultMap id="BaseResultMap" type="cn.vincent.crm.order.domain.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="quantity" property="quantity"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="amount" property="amount"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_id, product_id, product_name, quantity, unit_price, amount,
        create_user, update_user, create_time, update_time
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM crm_order_item
        WHERE id = #{id}
    </select>

    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM crm_order_item
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <insert id="insert" parameterType="cn.vincent.crm.order.domain.OrderItem">
        INSERT INTO crm_order_item (id, order_id, product_id, product_name, quantity, unit_price, amount,
                                    create_user, update_user, create_time, update_time)
        VALUES (#{id}, #{orderId}, #{productId}, #{productName}, #{quantity}, #{unitPrice}, #{amount},
                #{createUser}, #{updateUser}, #{createTime}, #{updateTime})
    </insert>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO crm_order_item (id, order_id, product_id, product_name, quantity, unit_price, amount,
                                    create_user, update_user, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.orderId}, #{item.productId}, #{item.productName}, #{item.quantity}, #{item.unitPrice}, #{item.amount},
             #{item.createUser}, #{item.updateUser}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <update id="update" parameterType="cn.vincent.crm.order.domain.OrderItem">
        UPDATE crm_order_item
        <set>
            <if test="orderId != null">order_id = #{orderId},</if>
            <if test="productId != null">product_id = #{productId},</if>
            <if test="productName != null">product_name = #{productName},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="unitPrice != null">unit_price = #{unitPrice},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM crm_order_item
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
